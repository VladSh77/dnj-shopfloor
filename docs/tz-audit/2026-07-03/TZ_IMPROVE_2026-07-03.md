# План покращення ТЗ — /Users/kobzar/Developer/Fayna-Workspace/Projects/dnj-shopfloor/docs/TZ.md

_Дата: 2026-07-03. Кожен рядок: знахідка → готове EARS-формулювання з методом верифікації → пріоритет MoSCoW. Правки застосовує людина/агент окремим кроком._

| Вимога | Проблема | Нове формулювання (EARS) | Верифікація | Пріоритет |
|---|---|---|---|---|
| REQ-001 | Відсутні деталі ідентифікації кіоска та механізму авто-логіну. Незрозумілі наслідки безпеки 'без пароля'. | WHEN a GET request is made to `/kiosk` from a pre-authorized kiosk, THEN the system SHALL automatically log in the associated kiosk account without requiring a password. The kiosk's authorization SHALL be verified using a unique, pre-registered client certificate. | Test (successful auto-login from authorized kiosk, denied access from unauthorized kiosk), Inspect (kiosk certificate management, server-side authorization logic). | Must |
| REQ-001 | Неверифіковано: відсутні критерії успіху та метод перевірки. Незрозуміло, що є 'авто-логіном'. | WHEN an authorized kiosk successfully accesses `/kiosk`, THEN the system SHALL establish an active session for the kiosk account, allowing subsequent requests to be processed under that session for a minimum of 60 minutes without further authentication. | Test (verify session establishment, check session validity after 60 minutes, attempt requests after session expiry), Inspect (session management configuration). | Must |
| REQ-001 | Не готова до Gherkin: відсутні умови, дії та очікувані результати. | IF an authorized kiosk sends a GET request to `/kiosk`, THEN the system SHALL respond with a 200 OK status, a valid session token, and the kiosk account SHALL be logged in, ready to process further requests. | Test (API response status code, presence and validity of session token, ability to make subsequent authenticated calls). | Must |
| REQ-002 | Критична неповнота: відсутні деталі безпеки (довжина PIN, політика повторів, блокування, використання солі, зберігання хешу). | WHEN an operator attempts to authenticate with a PIN, THEN the system SHALL:  1. Require the PIN to be exactly 4 digits long.  2. Store PINs as SHA-256 hashes with a unique, cryptographically secure salt for each operator.  3. Allow a maximum of 3 incorrect PIN attempts within a 5-minute window.  4. IF 3 incorrect attempts occur, THEN the operator account SHALL be locked for 15 minutes. | Inspect (code review for PIN length validation, hashing, salting, and lockout logic), Test (positive/negative PIN attempts, verify lockout mechanism after 3 failed attempts, check lockout duration). | Must |
| REQ-002 | Неверифіковано: відсутні критерії успішної/невдалої аутентифікації та метод перевірки безпекових аспектів. | IF an operator provides a correct PIN, THEN the system SHALL authenticate the operator and grant access within 1 second. IF an operator provides an incorrect PIN, THEN the system SHALL deny access, log the failed attempt, and respond within 1 second. The system SHALL use SHA-256 with a unique salt for PIN hashing. | Test (successful login, unsuccessful login with incorrect PIN, measure response times for both scenarios), Inspect (security logs for failed attempts, code review for hashing and salting implementation). | Must |
| REQ-002 | Не готова до Gherkin: відсутні умови (введення PIN), дії та очікувані результати (успіх/невдача). | WHEN an operator attempts to authenticate by entering a 4-digit PIN, THEN the system SHALL verify the PIN by comparing its SHA-256 hash against the stored hash. IF the hashes match, THEN authentication SHALL succeed; OTHERWISE, it SHALL fail. | test | Must |
| REQ-003 | Неповнота: відсутні деталі вмісту списку, визначення 'поточної машини', обробка порожнього списку, частота оновлення. | WHILE the system is assigned to a specific machine, THEN it SHALL display a list of all Work Orders for that machine, including Work Order ID, Status, and Due Date, refreshing every 60 seconds. IF no Work Orders are assigned, THEN the system SHALL display 'No Work Orders Found'. | inspect | Must |
| REQ-003 | Неверифіковано: відсутні критерії успішного відображення, порядок, вміст елементів списку. | WHEN the system displays Work Orders, THEN they SHALL be sorted by Due Date (ascending) and each entry SHALL clearly show Work Order ID, Status, and Due Date. The list SHALL load within 2 seconds. | test | Should |
| REQ-003 | Не готова до Gherkin: відсутні умови (машина визначена), дії (відображення) та очікувані результати (конкретний список). | GIVEN the system is assigned to a specific machine AND Work Orders are available for that machine, WHEN the system initializes, THEN it SHALL display the complete list of assigned Work Orders. | demo | Must |
| REQ-004 | Неповнота: відсутні деталі про вміст тестового друку, механізм запуску, очікуваний результат та обробку помилок друку. | WHEN an operator initiates a test print, THEN the system SHALL print a page containing 'Test Print Successful' and the current timestamp. IF the print fails, THEN the system SHALL notify the operator with an appropriate error message. | test | Should |
| REQ-004 | Неверифіковано: відсутні критерії успішного друку, очікуваний вигляд сторінки, поведінка при відмові принтера. | WHEN a user initiates a test print, THEN the system SHALL send a predefined test page to the default printer. IF the print is successful, THEN the system SHALL display a 'Print successful' message within 3 seconds. IF the printer is unavailable or fails, THEN the system SHALL display an 'Printer error: [error code]' message and NOT block the application. The test page SHALL contain specific text (e.g., 'Test Print Page - [Date/Time]') and a simple graphic. | Test (verify print output, error messages, timing); Inspect (test page content) | Must |
| REQ-004 | Не готова до Gherkin: відсутні умови (принтер доступний), дії (запуск друку) та очікувані результати (конкретний вихід). | WHEN a user selects 'Test Print' from the menu AND a printer is available and connected, THEN the system SHALL send a predefined test page to the default printer. The test page SHALL be printed completely and legibly, containing the text 'Test Print Page' and the current date/time. | Test (execute print, inspect physical output) | Must |
| REQ-005 | Неповнота: відсутні деталі про механізм запуску, необхідні передумови, зміни стану Work Order та обробку помилок. | WHEN a user selects an eligible Work Order AND clicks the 'Start Work' button, THEN the system SHALL change the Work Order's status from 'Pending' to 'In Progress'. The system SHALL record the start timestamp and the user who initiated the start. IF the Work Order is already 'In Progress' or 'Completed', THEN the system SHALL display an error message 'Work Order cannot be started' and NOT change its status. | Test (start WO, check status, check timestamps, test invalid states); Inspect (database for status/timestamp changes) | Must |
| REQ-005 | Неверифіковано: відсутні критерії успішного старту, очікувані зміни стану системи/Work Order. | WHEN a user successfully starts a Work Order, THEN the Work Order's status SHALL transition from 'Pending' to 'In Progress'. The system SHALL display a confirmation message 'Work Order [ID] started successfully' within 2 seconds. The Work Order's 'Started By' field SHALL be populated with the current user's ID, and the 'Start Time' field SHALL be populated with the current system timestamp. | Test (start WO, verify status change, check UI message); Inspect (database fields) | Must |
| REQ-005 | Не готова до Gherkin: відсутні умови (Work Order обрано), дії (старт) та очікувані результати (зміна статусу). | GIVEN a Work Order with status 'Pending' is selected by the user, WHEN the user clicks the 'Start Work' button, THEN the Work Order's status SHALL change to 'In Progress' AND the system SHALL display a success notification. | Test (execute scenario, observe status and notification) | Must |
| REQ-006 | Невизначений об'єкт 'робота'. Незрозуміло, що саме ставиться на паузу та які наслідки цієї дії. | WHEN a user initiates a 'pause' action on an active 'Task', THEN the 'Task' status SHALL change to 'Paused' and all associated processing for that 'Task' SHALL cease. | Test: Verify that an active task's status changes to 'Paused' and no further processing occurs for that task after the pause action. | Must |
| REQ-006 | Відсутні критерії верифікації та вимірності. Незрозуміло, як перевірити 'можливість поставити на паузу' та її успішність. | WHEN a user selects an active 'Task' and clicks the 'Pause' button, THEN the system SHALL update the 'Task' status to 'Paused' within 1 second and display a confirmation message 'Task [Task ID] paused successfully.' | Test: Verify that clicking 'Pause' on an active task updates its status to 'Paused' within 1 second and displays the specified confirmation message. | Must |
| REQ-006 | Відсутній опис поведінки системи під час паузи та можливих помилок. | WHILE a 'Task' is in 'Paused' status, THEN no automated processing or user-initiated actions (except 'Resume' or 'Cancel') SHALL be permitted for that 'Task'. IF an attempt is made to perform an unauthorized action on a paused task, THEN the system SHALL display an error message 'Action not allowed for paused task [Task ID]'. | Test: Verify that no unauthorized actions can be performed on a paused task and that appropriate error messages are displayed. | Should |
| REQ-007 | Невизначений об'єкт 'робота'. Незрозуміло, що саме відновлюється та які наслідки цієї дії. | WHEN a user initiates a 'resume' action on a 'Paused' 'Task', THEN the 'Task' status SHALL change to 'Active' and all previously suspended processing for that 'Task' SHALL recommence from the point of interruption. | Test: Verify that a paused task's status changes to 'Active' and its processing resumes from where it left off after the resume action. | Must |
| REQ-007 | Відсутні критерії верифікації та вимірності. Незрозуміло, як перевірити 'можливість відновити роботу'. | WHEN a user selects a 'Paused' 'Task' and clicks the 'Resume' button, THEN the system SHALL update the 'Task' status to 'Active' within 2 seconds and display a confirmation message 'Task [Task ID] resumed successfully.' | Test: Verify that clicking 'Resume' on a paused task updates its status to 'Active' within 2 seconds and displays the specified confirmation message. | Must |
| REQ-007 | Відсутній опис поведінки системи під час відновлення та можливих помилок. | WHEN a user initiates restoration of a paused Work Order, THEN the system SHALL restore the Work Order to its exact state prior to the pause within 5 seconds. IF restoration fails, THEN the system SHALL display an error message and keep the Work Order in the paused state. | Test: Pause a Work Order, then initiate restoration. Verify the Work Order's state is identical to pre-pause and restoration completes within 5 seconds. Simulate failure to verify error message and state retention. | Must |
| REQ-008 | Невизначене значення 'зупинити'. Чи це відрізняється від 'паузи'? Який кінцевий стан Work Order після зупинки? | WHEN a user initiates stopping a Work Order, THEN the system SHALL transition the Work Order to a 'Stopped' state, making it unavailable for further work. This 'Stopped' state is distinct from 'Paused' and SHALL prevent any further operations on the Work Order. | Test: Stop a Work Order. Verify its status changes to 'Stopped'. Attempt to resume or modify the Work Order to confirm unavailability. Inspect database state. | Must |
| REQ-008 | Відсутні критерії верифікації та вимірності. Незрозуміло, як перевірити 'можливість зупинити роботу'. | WHEN a user selects 'Stop Work Order' for an active Work Order, THEN the system SHALL display a confirmation prompt. IF the user confirms, THEN the Work Order's status SHALL change from 'Active' to 'Stopped' within 2 seconds, and all associated active timers SHALL cease. | Test: Initiate stop for an active Work Order. Verify confirmation prompt appears. Confirm and verify status change to 'Stopped' within 2 seconds. Check that all related timers are no longer running. | Must |
| REQ-008 | Відсутній опис поведінки системи під час зупинки та можливих помилок. | WHILE the system is processing a 'Stop Work Order' request, THEN the Work Order SHALL remain in its current state until the stop operation is complete. IF the stop operation fails, THEN the system SHALL display an error message 'Failed to stop Work Order. Please try again.' and keep the Work Order in its original state. | Test: Simulate a network error or backend failure during a 'Stop Work Order' request. Verify that the Work Order's state does not change and the specified error message is displayed. | Should |
| REQ-009 | Відсутні критерії верифікації та вимірності. Незрозуміло, як перевірити успішність виходу з системи (наприклад, інвалідація сесії). | WHEN a user selects 'Logout', THEN the system SHALL invalidate the user's current session, clear all session-related data, and redirect the user to the login page within 1 second. IF the user attempts to access a protected page after logout, THEN the system SHALL require re-authentication. | Test: Log out from the system. Verify redirection to the login page within 1 second. Attempt to access a protected URL directly; verify redirection to login or re-authentication prompt. | Must |
| REQ-009 | Недостатньо деталізовано поведінку системи при виході (наприклад, перенаправлення, очищення даних). | WHEN a user initiates logout, THEN the system SHALL clear all session-specific data and redirect the user to the login page. | Test (verify redirection to login page and absence of previous session data). | Must |
| REQ-010 | Невизначений термін 'не інкрементний' у контексті wall-clock, що відображає час. Потребує уточнення. | WHEN a specific operational period concludes, THEN the wall-clock timer SHALL display the total duration of that completed period as a static value, without continuous incrementation. | Test (observe timer after operational period conclusion; verify it remains static). | Must |
| REQ-010 | Відсутні критерії верифікації та вимірності (наприклад, точність, джерело часу, формат відображення). | WHEN the wall-clock timer displays the total operational time, THEN it SHALL be accurate to within ±1 second per hour, use the system's internal clock as its source, and display the time in HH:MM:SS format. | Test (compare displayed time with an external reference, check format). | Must |
| REQ-010 | Невизначений об'єкт 'робота'. Незрозуміло, для чого саме відображається загальний час. | The wall-clock timer SHALL display the total accumulated time that the system has been operational since its last successful boot. | Test (compare displayed time with system uptime logs/commands). | Must |
| REQ-011 | Вимога неповна: відсутні деталі щодо того, для якого виконання потрібен прогрес-бар та як саме він має базуватися на часі. Незрозуміло, що є 'часом виконання'. | WHEN the system executes the 'Data Export' operation, THEN a progress bar SHALL be displayed, visually representing the estimated completion time. The progress bar SHALL update at least every 1 second, with its progress calculated as (elapsed time / estimated total time) * 100%. | Test (initiate 'Data Export', observe progress bar updates and calculation). | Should |
| REQ-011 | Неверифікована: відсутні критерії успіху, порогові значення або метод верифікації. Неможливо перевірити, чи вимога виконана. | WHEN an operation is estimated to take longer than 5 seconds, THEN a progress bar SHALL be displayed. WHILE the operation is running, THEN the progress bar SHALL update its visual representation every 1 second, reflecting the estimated completion time based on the total estimated duration. | Test: Simulate operations lasting 6, 15, and 30 seconds. Verify the progress bar appears, updates every second, and reaches 100% upon completion. Demo: Show the progress bar in action for a typical long-running task. | Should |
| REQ-011 | Невимірна: відсутні кількісні показники для оцінки прогресу або часу. 'Базується на часі' занадто загально. | WHEN an operation is estimated to take longer than 5 seconds, THEN a progress bar SHALL be displayed. WHILE the operation is running, THEN the progress bar SHALL update its visual representation every 1 second, reflecting the estimated completion time based on the total estimated duration. | Test: Simulate operations lasting 6, 15, and 30 seconds. Verify the progress bar appears, updates every second, and reaches 100% upon completion. Demo: Show the progress bar in action for a typical long-running task. | Should |
| REQ-012 | Вимога неповна: не визначено, що саме зберігається в сесії та що означає 'відновлення'. Відсутні сценарії помилок (напр., закінчення терміну дії сесії). | WHEN an authenticated user refreshes the page or navigates away and returns within 30 minutes, THEN their authentication status and the last active application state (e.g., selected filters, unsaved draft content) SHALL be automatically restored. IF the user returns after 30 minutes, THEN they SHALL be prompted to re-authenticate. | Test: Log in, perform actions (e.g., apply filters, start typing in a form), refresh the page within 10 minutes, verify state is restored. Repeat after 35 minutes, verify re-authentication prompt. Inspect: Review session storage mechanisms and expiry logic. | Must |
| REQ-012 | Неверифікована: відсутні чіткі критерії для перевірки збереження та відновлення сесії. Неможливо написати тест. | WHEN an authenticated user refreshes the page or navigates away and returns within 30 minutes, THEN their authentication status and the last active application state (e.g., selected filters, unsaved draft content) SHALL be automatically restored. IF the user returns after 30 minutes, THEN they SHALL be prompted to re-authenticate. | Test: Log in, perform actions (e.g., apply filters, start typing in a form), refresh the page within 10 minutes, verify state is restored. Repeat after 35 minutes, verify re-authentication prompt. Inspect: Review session storage mechanisms and expiry logic. | Must |
| REQ-012 | Невимірна: відсутні кількісні показники, наприклад, тривалість сесії, обсяг даних, що зберігаються. | WHEN an authenticated user refreshes the page or navigates away and returns within 30 minutes, THEN their authentication status and the last active application state (e.g., selected filters, unsaved draft content) SHALL be automatically restored. IF the user returns after 30 minutes, THEN they SHALL be prompted to re-authenticate. | Test: Log in, perform actions (e.g., apply filters, start typing in a form), refresh the page within 10 minutes, verify state is restored. Repeat after 35 minutes, verify re-authentication prompt. Inspect: Review session storage mechanisms and expiry logic. | Must |
| REQ-013 | Вимога неповна: відсутні конкретні визначення 'адаптивного інтерфейсу', цільові розміри екранів та очікувана поведінка елементів на них. | WHEN the application is displayed on a screen with a width between 320px and 767px (inclusive), THEN the main content area MUST adopt a single-column layout, and vertical scrolling MUST be enabled if content height exceeds viewport height. WHEN the application is displayed on a screen with a width between 768px and 1023px (inclusive), THEN the main content area MUST adopt a two-column layout, and vertical scrolling MUST be enabled if content height exceeds viewport height. | Test by resizing browser window or using device emulators to 320px, 767px, 768px, 1023px, and 1024px, verifying layout and scroll functionality. | Must |
| REQ-013 | Неверифікована: неможливо перевірити 'адаптивність' без чітких критеріїв, наприклад, брейкпойнтів або очікуваних змін макету. | WHEN the application is displayed on a screen with a width between 320px and 767px (inclusive), THEN the main content area MUST adopt a single-column layout, and vertical scrolling MUST be enabled if content height exceeds viewport height. WHEN the application is displayed on a screen with a width between 768px and 1023px (inclusive), THEN the main content area MUST adopt a two-column layout, and vertical scrolling MUST be enabled if content height exceeds viewport height. | Test by resizing browser window or using device emulators to 320px, 767px, 768px, 1023px, and 1024px, verifying layout and scroll functionality. | Must |
| REQ-013 | Невимірна: відсутні кількісні показники для визначення 'малих екранів' або критеріїв адаптації. | WHEN the application is displayed on a screen with a width between 320px and 767px (inclusive), THEN the main content area MUST adopt a single-column layout, and vertical scrolling MUST be enabled if content height exceeds viewport height. WHEN the application is displayed on a screen with a width between 768px and 1023px (inclusive), THEN the main content area MUST adopt a two-column layout, and vertical scrolling MUST be enabled if content height exceeds viewport height. | Test by resizing browser window or using device emulators to 320px, 767px, 768px, 1023px, and 1024px, verifying layout and scroll functionality. | Must |
| REQ-014 | Вимога неповна: не визначено, що саме включає 'live статус', як часто він оновлюється та що відбувається у випадку відсутності зв'язку з машиною. | WHILE a user is viewing the "Machines Overview" screen, THEN the system MUST display the operational status (e.g., "Running", "Idle", "Offline"), current location (GPS coordinates), and last reported speed for each machine. The status data MUST be updated every 5 seconds. IF the connection to a machine is lost for more than 30 seconds, THEN its status MUST change to "Offline" and be highlighted in red. | Test by simulating machine status changes, network disconnections, and observing update frequency and display accuracy. Use network monitoring tools to confirm 5-second updates. | Must |
| REQ-014 | Неверифікована: відсутні чіткі критерії для 'live' (напр., максимальна затримка оновлення). Неможливо перевірити коректність відображення. | WHILE a user is viewing the "Machines Overview" screen, THEN the system MUST display the operational status (e.g., "Running", "Idle", "Offline"), current location (GPS coordinates), and last reported speed for each machine. The status data MUST be updated every 5 seconds. IF the connection to a machine is lost for more than 30 seconds, THEN its status MUST change to "Offline" and be highlighted in red. | Test by simulating machine status changes, network disconnections, and observing update frequency and display accuracy. Use network monitoring tools to confirm 5-second updates. | Must |
| REQ-014 | Невимірна: відсутні кількісні показники для визначення 'live' або частоти оновлення статусу. | WHEN the dashboard is loaded or refreshed, THEN the system SHALL display the operational status of all machines. WHILE a machine is operational, its status SHALL be updated on the dashboard every 5 seconds. The status SHALL indicate one of the following: 'Running', 'Idle', 'Maintenance', or 'Offline'. | Test: Verify that the status updates every 5 seconds and accurately reflects the machine's state (Running, Idle, Maintenance, Offline) for at least 3 distinct machines. | Must |
| REQ-015 | Вимога неповна: не визначено, яка дія або навігація відбувається після кліку на картку машини. Відсутній опис очікуваного результату. | IF a user clicks on a machine card on the dashboard, THEN the system SHALL navigate the user to the 'Machine Details' page for that specific machine. | Test: Click on 5 different machine cards and verify that each click successfully navigates to the correct 'Machine Details' page corresponding to the clicked machine. | Must |
| REQ-015 | Неверифікована: хоча клікабельність можна перевірити, відсутність опису результату кліку робить вимогу не повністю верифікованою. | IF a user clicks on a machine card on the dashboard, THEN the system SHALL navigate the user to the 'Machine Details' page for that specific machine. | Test: Click on 5 different machine cards and verify that each click successfully navigates to the correct 'Machine Details' page corresponding to the clicked machine. | Must |
| REQ-016 | Невизначеність "статистики": не вказано, які саме дані мають відображатися, їх джерело, формат та частота оновлення. | WHEN the 'Machine Details' page is displayed, THEN the system SHALL present a panel showing the machine's operational statistics. This panel SHALL include: Total uptime (HH:MM:SS) since last reset, Current production rate (units/hour), Daily error count, and Average temperature (°C) over the last hour. WHILE the 'Machine Details' page is active, THEN these statistics SHALL update every 10 seconds. | Inspect: Verify all specified statistics (uptime, production rate, error count, temperature) are displayed with correct units/format. Test: Verify statistics update every 10 seconds and reflect accurate data for a selected machine. | Must |
| REQ-016 | Відсутність критеріїв верифікації: неможливо перевірити коректність відображення без чітких метрик та очікувань. | WHEN the 'Machine Details' page is displayed, THEN the system SHALL present a panel showing the machine's operational statistics. This panel SHALL include: Total uptime (HH:MM:SS) since last reset, Current production rate (units/hour), Daily error count, and Average temperature (°C) over the last hour. WHILE the 'Machine Details' page is active, THEN these statistics SHALL update every 10 seconds. | Inspect: Verify all specified statistics (uptime, production rate, error count, temperature) are displayed with correct units/format. Test: Verify statistics update every 10 seconds and reflect accurate data for a selected machine. | Must |
| REQ-016 | Не EARS-формат: ускладнює автоматичний парсинг та генерацію тестів. | WHEN a user navigates to the machine details view, THEN the system SHALL display the machine's key operational statistics, including uptime, production count, and error rate. | Test (Verify displayed statistics match backend data sources for accuracy and completeness). | Must |
| REQ-017 | Невизначеність джерела та логіки: не вказано, як система визначає "поточного оператора" та розраховує "час роботи". | WHEN an operator is logged into a machine, THEN the system SHALL display the operator's ID and their total work time on that machine, calculated from their login timestamp to the current time. | Test (Verify displayed operator ID matches active login record; verify work time calculation against system logs). | Must |
| REQ-017 | Відсутність критеріїв верифікації: неможливо перевірити коректність відображення без чітких метрик та очікувань (наприклад, точність часу). | WHEN an operator is logged into a machine, THEN the system SHALL display the operator's ID and their total work time on that machine, accurate to within +/- 1 minute. | Test (Compare displayed work time with calculated time from system logs, ensuring accuracy within 1 minute tolerance). | Must |
| REQ-017 | Не EARS-формат: ускладнює автоматичний парсинг та генерацію тестів. | WHILE an operator is actively logged into a machine, THEN the system SHALL continuously update and display the operator's name and their cumulative work duration on that machine. | Inspect (Observe UI for continuous updates); Test (Verify cumulative duration calculation logic). | Must |
| REQ-018 | Невизначеність "прогресу": не вказано, що саме означає прогрес (відсоток, кількість кроків), як він розраховується та відображається. | WHEN a Work Order is active, THEN the system SHALL display its progress as a percentage, calculated by (completed steps / total steps) * 100, rounded to the nearest whole number. | Test (Verify displayed percentage for Work Orders with various completion states and total steps). | Must |
| REQ-018 | Відсутність критеріїв верифікації: неможливо перевірити коректність відображення без чітких метрик та очікувань. | WHEN a Work Order has a progress status, THEN the system SHALL display its progress as a percentage value from 0% to 100%. | Test: Verify the displayed percentage matches the calculated progress based on completed tasks. | Must |
| REQ-018 | Не EARS-формат: ускладнює автоматичний парсинг та генерацію тестів. | WHEN a Work Order's progress changes, THEN the system SHALL update its displayed progress as a percentage (0-100%) on the dashboard within 1 second. | Test: Observe dashboard for accurate and timely progress updates. | Must |
| REQ-019 | Невизначеність поведінки системи: не описано, що саме відбувається при паузі (зміна статусу, зупинка таймерів, вплив на Work Order). | WHEN a user selects 'Pause' for an active Work Order from the dashboard, THEN the system SHALL change the Work Order status to 'Paused', stop all associated timers, and prevent further progress updates until resumed. | Test: Verify status change, timer halt, and inability to update progress while paused. | Must |
| REQ-019 | Відсутність критеріїв верифікації: неможливо перевірити функціонал без чітких очікувань щодо станів системи до/після дії. | IF an active Work Order is paused by a user from the dashboard, THEN the system SHALL update its status to 'Paused' within 1 second and halt any active time tracking for that Work Order. | Test: Verify status change and timer cessation within the specified timeframe. | Must |
| REQ-019 | Не EARS-формат: ускладнює автоматичний парсинг та генерацію тестів. | WHEN a user clicks the 'Pause' button for an active Work Order on the dashboard, THEN the system SHALL immediately (within 500ms) update the Work Order's status to 'Paused' and suspend all associated time-tracking activities. | Test: Measure response time for status update and confirm timer suspension. | Must |
| REQ-021 | Вимога неповна: не вказано, які саме дані мають бути в моделі, її структура, зв'язки, та які події вона має логувати. | The `dnj.workorder.log` model SHALL be structured to include `event_type` (string), `timestamp` (datetime), `user_id` (integer, foreign key to user model), and `workorder_id` (integer, foreign key to workorder model, nullable). | Inspect database schema definition for `dnj.workorder.log` model to confirm specified fields and types. | Must |
| REQ-021 | Термін 'логінг подій' є занадто загальним, що робить вимогу неоднозначною щодо її функціонального обсягу. | The `dnj.workorder.log` model SHALL specifically log predefined workorder-related events as detailed in REQ-022, ensuring clarity of its functional scope. | Inspect model definition and associated event logging logic to confirm alignment with REQ-022. | Must |
| REQ-021 | Відсутній чіткий метод верифікації, крім факту створення моделі. Незрозуміло, як перевірити її коректність та відповідність призначенню. | The system SHALL successfully create the `dnj.workorder.log` database model. | Inspect the database to confirm the existence of the `dnj.workorder.log` table. | Must |
| REQ-022 | Вимога неповна: не вказано, які атрибути (наприклад, користувач, час, Workorder ID) повинні бути залоговані для кожної події. | WHEN a `login`, `test_print`, `start`, `pause`, `resume`, `stop`, or `logout` event occurs, THEN the system SHALL log an entry in `dnj.workorder.log` including the event type, the exact timestamp, the ID of the performing user, and the ID of the associated workorder (if applicable). | Test by triggering each specified event and inspecting the `dnj.workorder.log` entries for presence and correctness of all attributes. | Must |
| REQ-022 | Відсутній чіткий метод верифікації та критерії успішного логування (наприклад, формат даних, цілісність). | WHILE logging events, THEN each entry in `dnj.workorder.log` SHALL have a unique identifier, a timestamp in ISO 8601 format, valid `user_id` and `workorder_id` (if present), ensuring data integrity and non-repudiation. | Test by simulating multiple events and analyzing the `dnj.workorder.log` data for uniqueness, correct formatting, and adherence to integrity constraints. | Must |
| REQ-023 | Вимога вкрай неоднозначна: 'повна хронологія' не має чіткого визначення (що саме, як довго, з якою деталізацією?). | WHEN any user action (e.g., login, data modification) or system event (e.g., configuration change) occurs, THEN the system SHALL automatically record a detailed event log including timestamp, user ID, event type, affected entity ID, and relevant parameter changes. | Inspect (audit log structure and content for completeness and detail level). | Must |
| REQ-023 | Неможливо верифікувати без конкретних критеріїв (наприклад, період зберігання, вимоги до цілісності даних). | WHILE events are being recorded, THEN the system SHALL store each event log record immutably for a minimum of 5 years. | Test (attempt to modify/delete log entries older than 5 years), Inspect (database retention policies and actual log data retrieval from past periods). | Must |
| REQ-023 | Відсутні нефункціональні вимоги щодо зберігання (наприклад, термін зберігання, доступність, стійкість до втрати даних) (smell: vague_nfr). | WHILE event logs are stored, THEN they SHALL be available with 99.9% uptime and ensure zero data loss. | Analyze (system uptime reports, data backup/recovery procedures, disaster recovery tests). | Must |
| REQ-024 | Вимога неповна: не вказано, хто має право конфігурувати, які саме атрибути оператора можна змінювати, та як керуються PIN-коди (створення, скидання, вимоги до с | WHEN a user with 'Admin' role navigates to `DNJ Shopfloor → Configuration → Operators`, THEN the system SHALL allow them to create, view, modify (name, ID, assigned roles), and deactivate operator accounts. The system SHALL also enable setting and resetting operator PINs, enforcing a minimum length of 6 characters, including at least one digit and one letter. | Test (verify role-based access, test all CRUD operations for operators, test PIN complexity rules and reset functionality). | Must |
| REQ-024 | Не готова до Gherkin: відсутні чіткі дії та очікувані результати для автоматизованого тестування. | IF an 'Admin' user attempts to create a new operator via `DNJ Shopfloor → Configuration → Operators` with a PIN less than 6 characters or without a digit/letter, THEN the system SHALL display an error message 'PIN must be at least 6 characters long and contain at least one digit and one letter' and prevent operator creation. | Test (attempt to create operator with invalid PINs, verify error message and prevention). | Must |
| REQ-025 | Вимога неповна: не вказано, як саме відбувається прив'язка (UI/API), хто має дозволи, та які правила прив'язки (наприклад, один оператор до багатьох машин, чи н | WHEN a user with 'Manager' role accesses the 'Operator-Machine Assignment' screen in Odoo UI, THEN the system SHALL allow assigning one or more operators to a single Workcenter, and one operator to multiple Workcenters. | Test UI assignment, inspect user roles and assigned Workcenters. | Must |
| REQ-025 | Неоднозначність щодо правил прив'язки (кардинальність зв'язку, можливість перепризначення, одночасна робота на кількох машинах). | WHILE an operator is assigned to a Workcenter, THEN the system SHALL allow reassigning that operator to a different Workcenter, and allow an operator to be simultaneously assigned to a maximum of 3 Workcenters. | Test re-assignment functionality via UI; test simultaneous assignment limit (e.g., assign to 4 Workcenters and verify error). | Should |
| REQ-026 | Недостатньо деталізовано: які саме параметри моніторингу (IP, Modbus) конфігуруються? Відсутні критерії успіху та опис взаємодії з UI. | WHEN a user with 'Administrator' role accesses the 'Machine Monitoring Configuration' section in Odoo UI, THEN the system SHALL allow configuring the Workcenter's IP address, Modbus TCP port (default 502), and Modbus slave ID (1-247) for data acquisition. IF the configuration is saved successfully, THEN the system SHALL display a confirmation message 'Monitoring parameters updated'. | Test UI configuration input, inspect saved values in database, verify confirmation message display. | Must |
| REQ-026 | Не є сингулярною: 'IP, Modbus' може бути розбито на окремі вимоги для кожної конфігурації. | WHEN a user with 'Administrator' role configures a Workcenter's network settings via Odoo UI, THEN the system SHALL allow specifying and saving the Workcenter's static IP address, validating it against a standard IPv4 format (e.g., 192.168.1.100). | Test UI input for IP address, inspect saved value, test IPv4 format validation (e.g., invalid input should be rejected). | Must |
| REQ-026 | Відсутній метод верифікації та вимірні критерії успіху. Як перевірити, що конфігурація 'працює'? | IF a Workcenter's monitoring parameters (IP, Modbus port, slave ID) are configured and saved successfully via Odoo UI, THEN the system SHALL initiate data acquisition from the Workcenter within 10 seconds, and display the first received data point (e.g., current machine status or counter value) on the 'Workcenter Dashboard'. | Test (configure parameters, then observe 'Workcenter Dashboard' for data within 10 seconds), Demo. | Must |
| REQ-027 | Неповна: не вказано, коли (тригер, частота) Machine Bridge повинен читати дані та що робити у випадку помилок зв'язку з Odoo. | WHEN the Machine Bridge starts, and WHILE it is running, it SHALL read the list of machines and their IP addresses from Odoo every 60 seconds. IF Odoo connection fails, THEN the Machine Bridge SHALL log the error and retry after 10 seconds, using the last known configuration. | Test: Simulate Odoo connection failure and verify logging and retry mechanism. Inspect: Verify the list of machines and IP addresses read from Odoo matches the source. | Must |
| REQ-028 | Неповна: не вказано частоту виконання ping, таймаут, критерії визначення 'онлайн/офлайн' (наприклад, кількість успішних відповідей). | WHILE the Machine Bridge is running, for each configured machine, it SHALL perform an ICMP ping every 10 seconds with a timeout of 1000ms. IF at least 3 out of 5 consecutive pings are successful, THEN the machine SHALL be considered 'online'; otherwise, it SHALL be considered 'offline'. | Test: Simulate machines going online/offline and verify status changes based on ping results and criteria. Inspect: Verify ping parameters (frequency, timeout). | Must |
| REQ-029 | Неповна: не вказано, з якої машини читаються регістри, коли (тригер, частота) та що робити у випадку помилок зв'язку Modbus. | WHILE a machine is 'online', the Machine Bridge SHALL read Modbus TCP registers HR[0-2] (status, speed, counter) from that machine every 5 seconds. IF a Modbus TCP connection error occurs for a machine, THEN the Machine Bridge SHALL log the error, mark the machine's Modbus status as 'unavailable', and retry after 15 seconds. | Test: Simulate Modbus connection errors and verify logging, status update, and retry. Inspect: Verify register values are read correctly from a live machine. | Must |
| REQ-030 | Недостатньо деталізовано: які саме 'результати моніторингу' відправляються? Відсутні специфікації REST API (endpoint, формат даних, аутентифікація). | WHEN monitoring results (machine status, Modbus register values) are updated or every 30 seconds, THEN the Machine Bridge SHALL send these results to Odoo via a POST request to '/api/v1/machine_monitoring' using JSON format {"machine_id": "...", "status": "...", "speed": "...", "counter": "..."}. The request SHALL be authenticated using an API key in the 'Authorization' header. | Inspect: Review API documentation and code for endpoint, format, and authentication. Test: Send sample data and verify Odoo receives and processes it correctly. | Must |
| REQ-030 | Відсутній метод верифікації та вимірні критерії успіху. Як перевірити, що відправка була успішною і дані коректно отримані Odoo? | IF the Machine Bridge sends monitoring results to Odoo via REST API, THEN it SHALL expect an HTTP 200 OK response within 5 seconds. IF any other HTTP status code is received or the timeout is exceeded, THEN the Machine Bridge SHALL log the error, mark the data as 'unsent', and retry the transmission up to 3 times with a 10-second delay between retries. | Test: Simulate various API responses (200, 400, 500, timeout) and verify logging, retry logic, and data marking. Analyze: Review logs for successful/failed transmissions. | Must |
| REQ-030 | Неповна: не вказано, коли (тригер, частота) відбувається відправка та що робити у випадку помилок зв'язку з Odoo API. | WHEN a monitoring cycle completes, THEN the system SHALL send its results to Odoo via REST API. IF the Odoo API call fails, THEN the system SHALL log the error and retry up to 3 times with a 5-second delay between retries. | Test (simulate API failure, verify retries and logging), Inspect (logs). | Must |
| REQ-031 | Не визначено, які саме дані потрібно опитувати та звідки. Нечітке визначення 'постійний' та 'безперервний режим'. | WHILE the system is operational, THEN it SHALL poll configured data points from connected devices every 30 seconds. | Test (verify polling interval and data acquisition), Inspect (logs/UI showing polled data). | Must |
| REQ-032 | Нечітке визначення 'одного циклу моніторингу' – що він включає, які кроки, критерії завершення. | IF the system is started with the `--test` flag, THEN it SHALL perform a single monitoring cycle (poll data, process data, send results to Odoo) and then terminate. | Test (run with `--test` flag, verify data polled, processed, sent, and system exits). | Should |
| REQ-033 | Відсутні деталі конфігурації Docker Compose, які компоненти Machine Bridge розгортаються, та критерії успішного розгортання. | WHEN the `docker-compose up` command is executed, THEN the system SHALL deploy all required Machine Bridge components (e.g., application service, database, data acquisition module) and ensure they are running and accessible. | Inspect (Docker Compose file, `docker ps`), Test (verify services are running and accessible via health checks). | Must |
| REQ-034 | Не визначено, які саме аспекти тестування будуть покриті PLC Simulator, та що таке `demo-industrial-iot`. | WHEN the PLC Simulator is used with the `demo-industrial-iot` configuration, THEN it SHALL simulate industrial IoT device data to enable testing of the Machine Bridge's data acquisition, processing, and integration with Odoo. | Demo (show simulator generating data and Machine Bridge processing it), Test (run specific test cases against the simulator). | Should |
| REQ-035 | Вимога є надто загальною. Повністю відсутні деталі щодо функціональності ендпоінту, його вхідних/вихідних даних, параметрів, авторизації, обробки помилок. | WHEN an unauthenticated GET request is made to /kiosk, THEN the system SHALL return a 401 Unauthorized status code. WHEN an authenticated GET request is made to /kiosk with a valid 'kiosk_id' parameter, THEN the system SHALL return a 200 OK status code and a JSON object containing 'id', 'name', 'location', 'status', and 'last_sync_time'. IF 'kiosk_id' is missing or invalid, THEN return a 400 Bad Request. IF no kiosk is found, THEN return a 404 Not Found. | test | Must |
| REQ-035 | Неможливо верифікувати коректність роботи без визначення очікуваної поведінки та форматів даних. | WHEN a successful GET request is made to /kiosk with a valid 'kiosk_id', THEN the system SHALL return a JSON object where 'id' is an integer, 'name' is a string (max 100 chars), 'location' is a string (max 200 chars), 'status' is an enum ('active', 'inactive', 'maintenance'), and 'last_sync_time' is an ISO 8601 datetime string. The response time SHALL be less than 200ms for 95% of requests. | test | Must |
| REQ-036 | Відсутній опис функціональності: не вказано призначення ендпоінту, вхідні/вихідні дані, очікувана поведінка та бізнес-логіка. | WHEN a POST request is made to /dnj_shopfloor/authenticate, IF the request body contains valid 'username' and 'password' credentials, THEN the system SHALL return a 200 OK status code and a JSON object containing an 'access_token' (JWT string) and 'expires_in' (integer, 3600 seconds). IF credentials are invalid, THEN return a 401 Unauthorized with message 'Invalid credentials'. IF body is malformed, THEN return a 400 Bad Request. | test | Must |
| REQ-036 | Неможливо верифікувати: відсутні критерії приймання, методи верифікації та кількісні пороги для тестування. | WHEN a successful POST request is made to /dnj_shopfloor/authenticate, THEN the 'access_token' in the response SHALL be a valid JWT with an expiration time of exactly 3600 seconds. The authentication process SHALL complete within 500ms for 99% of requests. The system SHALL log all failed authentication attempts, retaining logs for at least 30 days. | test, inspect | Must |
| REQ-036 | Відсутня пріоритизація (MoSCoW) та неможливість конвертації в Gherkin-сценарій для автоматизованого тестування. | GIVEN a user with valid credentials ('testuser', 'password123'), WHEN a POST request is sent to /dnj_shopfloor/authenticate with these credentials, THEN the system SHALL respond with a 200 OK status code AND the response body SHALL contain a non-empty 'access_token' string AND the 'expires_in' value SHALL be 3600. | test | Must |
| REQ-037 | Відсутній опис функціональності: не вказано призначення ендпоінту, вхідні/вихідні дані, очікувана поведінка та бізнес-логіка. | WHEN a client sends a POST request to `/dnj_shopfloor/session/open` with a valid `device_id` (string, max 50 chars) and `user_id` (string, max 50 chars) in the request body, THEN the system MUST create a new active session, return a unique `session_id` (UUID) and HTTP status `200 OK` within 300ms. | test | Must |
| REQ-037 | Неможливо верифікувати: відсутні критерії приймання, методи верифікації та кількісні пороги для тестування. | IF a POST request to `/dnj_shopfloor/session/open` contains an invalid or missing `device_id` or `user_id`, THEN the system MUST respond with HTTP status `400 Bad Request` and a descriptive error message, within 200ms. | test | Must |
| REQ-037 | Відсутня пріоритизація (MoSCoW) та неможливість конвертації в Gherkin-сценарій для автоматизованого тестування. | WHILE a session is active, IF a subsequent POST request to `/dnj_shopfloor/session/open` is made with the same `device_id` and `user_id`, THEN the system SHOULD either return the existing `session_id` or a new `session_id` after invalidating the previous one, ensuring no more than 1 active session per `device_id`/`user_id` pair. The response MUST be within 400ms. | test | Should |
| REQ-038 | Відсутній опис функціональності: не вказано призначення ендпоінту, вхідні/вихідні дані, очікувана поведінка та бізнес-логіка. | WHEN a client sends a POST request to `/dnj_shopfloor/session/action` with a valid `session_id` (UUID), `action_type` (enum: 'start_work', 'end_work', 'pause'), and `action_data` (JSON object) in the request body, THEN the system MUST record the action associated with the session and return HTTP status `200 OK` within 300ms. | test | Must |
| REQ-038 | Неможливо верифікувати: відсутні критерії приймання, методи верифікації та кількісні пороги для тестування. | IF a POST request to `/dnj_shopfloor/session/action` is made with an invalid or expired `session_id`, THEN the system MUST respond with HTTP status `401 Unauthorized` and an appropriate error message, within 250ms. | test | Must |
| REQ-038 | Відсутня пріоритизація (MoSCoW) та неможливість конвертації в Gherkin-сценарій для автоматизованого тестування. | WHEN a client sends a request to `POST /dnj_shopfloor/session/action` with valid session action data, THEN the system SHALL process the action, update the session state if required, and return an HTTP 200 OK response within 300 ms. | Test (API testing for various action types, verify session state changes, measure response time, Gherkin scenario conversion) | Must |
| REQ-039 | Відсутній опис функціональності: не вказано призначення ендпоінту, вхідні/вихідні дані, очікувана поведінка та бізнес-логіка. | WHEN a client sends a request to `POST /dnj_shopfloor/session/status` with a valid session ID and a new status, THEN the system SHALL update the session's status to the provided value and return an HTTP 200 OK response within 200 ms. | Test (API testing, verify session status update in backend, check response body for confirmation) | Must |
| REQ-039 | Неможливо верифікувати: відсутні критерії приймання, методи верифікації та кількісні пороги для тестування. | WHEN a client sends a request to `POST /dnj_shopfloor/session/status` with a valid session ID and a new status, THEN the system SHALL update the session's status to the provided value and return an HTTP 200 OK response within 200 ms. | Test (API testing, verify status update, measure response time, check HTTP status code 200) | Must |
| REQ-039 | Відсутня пріоритизація (MoSCoW) та неможливість конвертації в Gherkin-сценарій для автоматизованого тестування. | WHEN a client sends a request to `POST /dnj_shopfloor/session/status` with a valid session ID and a new status, THEN the system SHALL update the session's status to the provided value and return an HTTP 200 OK response within 200 ms. | Test (API testing, verify status update, measure response time, Gherkin scenario conversion for automated testing) | Must |
| REQ-040 | Відсутній опис функціональності: не вказано призначення ендпоінту, вхідні/вихідні дані, очікувана поведінка та бізнес-логіка. | WHEN a client sends a request to `POST /dnj_shopfloor/workorders` with valid work order details (e.g., product_id, quantity, due_date), THEN the system SHALL create a new work order, assign a unique ID, and return an HTTP 201 Created response with the new work order's ID within 500 ms. | Test (API testing, verify work order creation in DB, check response data for new ID, measure response time) | Must |
| REQ-040 | Неможливо верифікувати: відсутні критерії приймання, методи верифікації та кількісні пороги для тестування. | WHEN a valid POST request is sent to `/dnj_shopfloor/workorders` with a well-formed Work Order payload, THEN the system SHALL successfully create the Work Order, respond with a 201 Created status code, and complete the operation within 200ms. | test | Must |
| REQ-040 | Відсутня пріоритизація (MoSCoW) та неможливість конвертації в Gherkin-сценарій для автоматизованого тестування. | WHEN a valid Work Order is submitted via POST to `/dnj_shopfloor/workorders`, THEN the system SHALL successfully process the request, create the Work Order, and return a 201 Created status code with the new Work Order ID in the response body within 200ms. | test | Must |
| REQ-041 | Вимога неповна: відсутній опис призначення ендпоінту, вхідних/вихідних даних, очікуваної поведінки та обробки помилок. | WHEN a POST request is made to `/dnj_shopfloor/workcenters` with a valid Work Center payload (e.g., `name`, `location`, `capacity`), THEN the system SHALL create a new Work Center, assign a unique ID, and return a 201 Created status code with the new Work Center's details within 250ms. IF the payload is invalid, THEN the system SHALL return a 400 Bad Request status code with a descriptive error message. | test | Must |
| REQ-041 | Неможливо верифікувати: відсутні критерії успіху, метод верифікації та вимірні показники. | WHEN a valid Work Center creation request is sent to `/dnj_shopfloor/workcenters`, THEN the system SHALL successfully persist the Work Center data, respond with a 201 Created status code, and ensure the new Work Center is retrievable via GET within 500ms of creation. The response time for the POST request SHALL be under 250ms. | test | Must |
| REQ-041 | Відсутній опис API-контракту (схема запиту/відповіді), обробки помилок та NFRs. | WHILE processing a POST request to `/dnj_shopfloor/workcenters`, IF the request body conforms to the Work Center creation schema (e.g., `name: string`, `location: string`, `capacity: integer > 0`), THEN the system SHALL create the Work Center, return a 201 Created status code with the created Work Center's full schema, and complete the operation within 250ms. IF the schema is violated, THEN a 400 Bad Request SHALL be returned with a detailed validation error message. | test | Must |
| REQ-042 | Вимога неповна: відсутній опис призначення ендпоінту, вхідних/вихідних даних, очікуваної поведінки та обробки помилок. | WHEN a POST request is sent to `/dnj_shopfloor/dashboard` with a JSON body containing `startDate` and `endDate` (ISO 8601 format), THEN the system SHALL return a JSON object with aggregated production metrics for that period. IF the dates are invalid, THEN a 400 Bad Request SHALL be returned. | Test (Send valid/invalid requests, verify response data and status codes). | Must |
| REQ-042 | Неможливо верифікувати: відсутні критерії успіху, метод верифікації та вимірні показники. | WHEN a valid POST request is sent to `/dnj_shopfloor/dashboard`, THEN the system SHALL respond with a 200 OK status code and a JSON payload within 500ms for 95% of requests, indicating successful retrieval of dashboard data. | Test (Automated performance tests, verify status code and response time). | Must |
| REQ-042 | Відсутній опис API-контракту (схема запиту/відповіді), обробки помилок та NFRs. | WHILE processing requests to `/dnj_shopfloor/dashboard`, the system SHALL validate request and response payloads against the defined OpenAPI schema, and IF any validation fails or an internal error occurs, THEN it SHALL return an appropriate HTTP error status code (e.g., 400, 401, 403, 404, 422, 500) with a standardized error message format. | Inspect (OpenAPI documentation), Test (API tests for various error conditions). | Must |
| REQ-043 | Вимога неповна: відсутній опис призначення ендпоінту, вхідних/вихідних даних, очікуваної поведінки та обробки помилок. | WHEN a POST request is sent to `/dnj_shopfloor/machine/config` with a JSON body containing `machineId` (UUID) and `configuration` (JSON object), THEN the system SHALL update the specified machine's configuration. IF the `machineId` is not found, THEN a 404 Not Found SHALL be returned. | Test (Send valid/invalid config updates, verify database changes and status codes). | Must |
| REQ-043 | Неможливо верифікувати: відсутні критерії успіху, метод верифікації та вимірні показники. | WHEN a valid POST request successfully updates a machine's configuration via `/dnj_shopfloor/machine/config`, THEN the system SHALL respond with a 200 OK status code and a confirmation message within 300ms, and the updated configuration SHALL be persistent and retrievable. | Test (Automated API tests to check status code, response time, and subsequent GET requests to verify persistence). | Must |
| REQ-043 | Відсутній опис API-контракту (схема запиту/відповіді), обробки помилок та NFRs. | WHEN a client sends a POST request to `/dnj_shopfloor/machine/config`, THEN the API MUST provide a complete OpenAPI 3.x specification including request/response schemas, all possible error responses (e.g., 400, 401, 403, 404, 500), and documentation of non-functional requirements (e.g., expected latency, throughput, security). | Inspect (OpenAPI spec review), Test (API calls against spec). | Must |
| REQ-044 | Вимога неповна: відсутній опис призначення ендпоінту, вхідних/вихідних даних, очікуваної поведінки та обробки помилок. | WHEN a client sends a POST request to `/dnj_shopfloor/machine/heartbeat`, THEN the system MUST update the `last_active_timestamp` for the specified machine ID and respond with a `200 OK` status within 50ms. The request body MUST contain the `machine_id` (UUID) and MAY contain `status` (string). Error responses (e.g., 400 for invalid input, 404 for unknown machine) MUST be handled and documented. | Test (send requests, verify database update and response), Inspect (documentation). | Must |
| REQ-044 | Неможливо верифікувати: відсутні критерії успіху, метод верифікації та вимірні показники. | IF a POST request to `/dnj_shopfloor/machine/heartbeat` receives a `200 OK` response, THEN the `last_active_timestamp` for the corresponding machine in the database MUST be updated to the request processing time with an accuracy of ±1 second. The endpoint MUST handle at least 100 requests per second with a p90 latency below 100ms. | Test (automated tests verifying timestamp update and response codes), Analyze (performance testing reports). | Must |
| REQ-044 | Відсутній опис API-контракту (схема запиту/відповіді), обробки помилок та NFRs. | WHEN a client sends a POST request to `/dnj_shopfloor/machine/heartbeat`, THEN the API MUST provide a complete OpenAPI 3.x specification including request/response schemas (e.g., `machine_id` as UUID, `status` as string), all possible error responses (e.g., 400, 404), and documentation of non-functional requirements (e.g., expected latency, throughput). | Inspect (OpenAPI spec review), Test (API calls against spec). | Must |
| REQ-045 | Вимога неповна: відсутній опис призначення ендпоінту, вхідних/вихідних даних, очікуваної поведінки та обробки помилок. | WHEN a client sends a POST request to `/dnj_shopfloor/machine/stats`, THEN the system MUST record the provided operational statistics for the specified machine ID and respond with a `202 Accepted` status within 100ms. The request body MUST contain `machine_id` (UUID) and a structured `stats_payload` (JSON object with various metrics like `uptime_seconds`, `production_count`, `error_rate`). Error responses (e.g., 400 for invalid input, 404 for unknown machine) MUST be handled and documented. | Test (send requests, verify data storage and response), Inspect (documentation). | Must |
| REQ-045 | Неможливо верифікувати: відсутні критерії успіху, метод верифікації та вимірні показники. | WHEN a `POST /dnj_shopfloor/machine/stats` request is sent with valid data, THEN the system SHALL respond with a 200 OK status code and a JSON payload containing aggregated machine statistics within 500 ms. | test | Must |
| REQ-045 | Відсутній опис API-контракту (схема запиту/відповіді), обробки помилок та NFRs. | WHILE the `POST /dnj_shopfloor/machine/stats` endpoint is being developed, THEN its API contract, including request/response schemas, error handling (e.g., 400 for invalid input, 500 for server errors), and non-functional requirements (e.g., response time < 500ms, authentication), SHALL be fully documented in an OpenAPI specification. | inspect | Must |
| REQ-046 | Вимога неповна: відсутні функціональні та нефункціональні деталі моделі, її поля, бізнес-логіка, взаємодія з іншими компонентами. | WHEN the Odoo model `dnj.operator` is implemented, THEN it SHALL include fields for `name` (string, required), `employee_id` (integer, unique), `pin_hash` (string, SHA-256, required), and `is_active` (boolean, default true), with business logic to validate PINs against `pin_hash` during login attempts. | inspect | Must |
| REQ-046 | Неверифікована: відсутні критерії приймання та метод верифікації. Неможливо перевірити, чи модель 'для операторів' відповідає очікуванням. | WHEN the Odoo model `dnj.operator` is developed, THEN it SHALL successfully support creation, retrieval, update, and deletion of operator records, and correctly validate operator PINs (SHA-256) for at least 99% of test cases. | test | Must |
| REQ-046 | Неоднозначна: 'для операторів' не визначає чітко призначення та поведінку моделі. 'PIN (SHA-256)' є технічною деталлю, а не поведінкою. | The Odoo model `dnj.operator` SHALL serve as the central entity for managing shop floor operators, providing a secure mechanism for operator authentication using a SHA-256 hashed PIN, and storing essential operator profile information. | inspect | Must |
| REQ-047 | Вимога неповна: відсутні деталі станів, переходів, бізнес-логіки state machine, полів моделі та її функціональності. | WHEN a user initiates a kiosk interaction, the system SHALL create a `dnj.kiosk.session` record. This record SHALL include `user_id`, `start_time`, `end_time` (nullable initially), and a `state` field. The session lifecycle SHALL be managed by a state machine with the following states: 'Draft' (initial), 'Active', 'Paused', 'Ended', 'Cancelled'. IF a session is confirmed, THEN its state SHALL transition from 'Draft' to 'Active'. IF a session is explicitly ended, THEN its state SHALL transition from 'Active' to 'Ended', and `end_time` SHALL be recorded. | Inspect: Review the `dnj.kiosk.session` model definition in Odoo, verifying the presence of specified fields and the state machine configuration including states and transitions. | Must |
| REQ-047 | Неверифікована: відсутні критерії приймання та метод верифікації. Неможливо перевірити коректність управління сесіями. | WHEN a user initiates a kiosk interaction, the system SHALL create a `dnj.kiosk.session` record with an initial `state` of 'Draft'. IF the user confirms the session, THEN the system SHALL transition its state to 'Active'. IF the user terminates the session, THEN the system SHALL transition its state to 'Ended' and record the `end_time`. The system SHALL ensure that only valid state transitions are permitted. | Test: Create a session, verify 'Draft' state. Confirm session, verify 'Active' state. End session, verify 'Ended' state and `end_time` is recorded. Attempt invalid transitions (e.g., 'Draft' to 'Ended') and verify rejection. | Must |
| REQ-047 | Неоднозначна: 'для управління сесіями' не визначає чітко призначення та поведінку моделі. 'state machine' є технічною деталлю. | The `dnj.kiosk.session` model SHALL precisely track the lifecycle of a user's interaction with the kiosk, from initiation to completion or cancellation. This includes managing distinct operational states such as 'Draft', 'Active', 'Paused', 'Ended', and 'Cancelled', and defining the specific conditions under which transitions between these states occur. | Inspect: Review the documentation and implementation of the `dnj.kiosk.session` model to confirm that all defined states and transitions are clearly documented and logically implemented, reflecting the business process for kiosk interactions. | Must |
| REQ-048 | Вимога неповна: відсутні деталі полів моделі, бізнес-логіки запису пауз, їх типів, тривалості та взаємодії з іншими системами. | WHEN a user initiates a pause during an active kiosk session, the system SHALL create a `dnj.kiosk.pause` record. This record SHALL include `session_id` (linking to `dnj.kiosk.session`), `pause_type` (e.g., 'Break', 'Lunch', 'Technical'), `start_time`, and `end_time` (nullable initially). WHILE a pause is active, the system SHALL prevent further user interactions until the pause is ended. IF the pause is ended, THEN the system SHALL record the `end_time`. | Inspect: Review the `dnj.kiosk.pause` model definition, verifying fields and relationships. Test: Initiate a session, then a pause of a specific type, verify `dnj.kiosk.pause` record creation and `start_time`. End pause, verify `end_time` is recorded. | Must |
| REQ-048 | Неверифікована: відсутні критерії приймання та метод верифікації. Неможливо перевірити коректність запису пауз. | WHEN a user requests a pause during an active `dnj.kiosk.session`, the system SHALL create a `dnj.kiosk.pause` record, linking it to the current session via `session_id`. The record SHALL capture the `start_time` of the pause and allow for a `pause_type` to be specified. IF the user resumes the session, THEN the system SHALL update the corresponding `dnj.kiosk.pause` record with the `end_time`. | Test: Start a kiosk session, then trigger a 'Break' pause. Verify a `dnj.kiosk.pause` record is created with correct `session_id`, `pause_type`, and `start_time`. Resume the session and verify the `end_time` is accurately recorded in the pause record. Analyze: Review database entries for `dnj.kiosk.pause` to ensure data integrity. | Must |
| REQ-048 | Неоднозначна: 'для запису пауз' не визначає чітко призначення та поведінку моделі. | WHEN a user initiates a pause from the kiosk, THEN the `dnj.kiosk.pause` model MUST record a pause entry. This entry MUST include the `user_id`, `start_time`, `end_time` (nullable until pause ends), and `reason_id` (e.g., 'lunch', 'break', 'maintenance'). The system MUST ensure that a pause cannot exceed 60 minutes without supervisor approval. | Test: Initiate pauses of various types and durations from the kiosk, verify correct data storage in the `dnj.kiosk.pause` model. Test: Attempt to exceed 60 minutes without approval. | Must |
| REQ-049 | Вимога неповна: відсутні деталі типів подій, що логуються, їх структури, політики зберігання, обсягів та вимог до продуктивності. | WHEN a work order's status changes (e.g., 'new' to 'in_progress') or a critical user action occurs (e.g., 'start', 'complete', 'reject'), THEN the `dnj.workorder.log` model MUST record an event entry within 100ms. Each entry MUST include: `timestamp`, `work_order_id`, `event_type` (e.g., 'status_change', 'started', 'completed'), `user_id`, `description`, and `details` (JSONB, e.g., `{'old_status': 'new', 'new_status': 'in_progress'}`). The system MUST retain log entries for a minimum of 365 days. | Test: Perform various work order actions and verify the presence, correctness, and completeness of log entries in the database. Analyze: Check log retention policy implementation. | Must |
| REQ-049 | Неверифікована: відсутні критерії приймання та метод верифікації. Неможливо перевірити коректність функціонування Event Log. | WHEN a work order's status changes (e.g., 'new' to 'in_progress') or a critical user action occurs (e.g., 'start', 'complete', 'reject'), THEN the `dnj.workorder.log` model MUST record an event entry within 100ms. Each entry MUST include: `timestamp`, `work_order_id`, `event_type` (e.g., 'status_change', 'started', 'completed'), `user_id`, `description`, and `details` (JSONB, e.g., `{'old_status': 'new', 'new_status': 'in_progress'}`). The system MUST retain log entries for a minimum of 365 days. | Test: Perform various work order actions and verify the presence, correctness, and completeness of log entries in the database. Analyze: Check log retention policy implementation. Inspect: Review model definition for required fields. | Must |
| REQ-049 | Неоднозначна: 'для Event Log' не визначає чітко призначення та поведінку моделі. | WHEN a work order's status changes (e.g., 'new' to 'in_progress') or a critical user action occurs (e.g., 'start', 'complete', 'reject'), THEN the `dnj.workorder.log` model MUST record an event entry within 100ms. Each entry MUST include: `timestamp`, `work_order_id`, `event_type` (e.g., 'status_change', 'started', 'completed'), `user_id`, `description`, and `details` (JSONB, e.g., `{'old_status': 'new', 'new_status': 'in_progress'}`). The system MUST retain log entries for a minimum of 365 days. | Test: Perform various work order actions and verify the presence, correctness, and completeness of log entries in the database. Inspect: Review model definition and field types. | Must |
| REQ-050 | Вимога неповна: відсутні деталі параметрів статусу, частоти оновлення, конкретних Modbus регістрів, бізнес-логіки обробки даних. | WHILE machines are operational, THEN the `dnj.machine.status` model MUST store the latest status data for each monitored machine. WHEN a Modbus register value changes or every 5 seconds (whichever is sooner), THEN the system MUST update the corresponding machine's status in the `dnj.machine.status` model. The model MUST include fields for: `machine_id`, `timestamp_last_update`, `operational_status` (e.g., 'running', 'idle', 'error'), `modbus_register_1_value` (e.g., integer), `modbus_register_2_value` (e.g., float), and `network_status` (e.g., 'online', 'offline'). | Test: Simulate Modbus data changes and network disconnections, then verify model updates and data accuracy. Inspect: Review model fields, data types, and Modbus integration configuration. Analyze: Monitor update frequency and data latency. | Must |
| REQ-050 | Неверифікована: відсутні критерії приймання та метод верифікації. Неможливо перевірити коректність моніторингу та збору даних. | WHEN a machine's network status or Modbus data changes, THEN the Odoo model `dnj.machine.status` SHALL store the updated network connectivity status (online/offline, last seen timestamp) and the latest Modbus register values (e.g., temperature, pressure, voltage) for that machine. | Test: Simulate status/data changes for 3 machines and verify the `dnj.machine.status` records are created/updated correctly in Odoo. | Must |
| REQ-050 | Неоднозначна: 'для мережевого моніторингу та Modbus даних' не визначає чітко призначення та поведінку моделі. | WHILE a machine is being monitored, the Odoo model `dnj.machine.status` SHALL contain fields for `machine_id`, `is_online` (boolean), `last_seen` (datetime), and at least 5 configurable Modbus register value fields (e.g., `temp_c`, `pressure_bar`) with their respective `timestamp_collected`. | Inspect: Review the Odoo model definition (`dnj.machine.status`) to confirm the presence and types of specified fields. | Must |
| REQ-051 | Вимога нечітка: 'Конфігурація' не визначає, що саме має бути налаштовано, які параметри чи цілі. Неможливо верифікувати без критеріїв. | WHEN deploying the system, THEN the `docker-compose.yml` file SHALL define two services: `odoo` (using Odoo version 16.0) and `db` (using PostgreSQL version 15.x), configured to allow `odoo` to connect to `db` via a shared network. | Inspect: Review `docker-compose.yml` for service definitions, image versions, and network configuration. | Must |
| REQ-051 | Відсутній метод верифікації та критерії успіху. Як перевірити, що конфігурація 'правильна' чи 'достатня'? | WHEN `docker-compose up` is executed, THEN the Odoo service SHALL successfully connect to the PostgreSQL 15.x database service, create its initial database, and persist all Odoo data across container restarts. | Test: Run `docker-compose up -d`, create a test record in Odoo, then `docker-compose down`, then `docker-compose up -d` again, and verify the test record persists. | Must |
| REQ-051 | Відсутня пріоритезація (MoSCoW). | WHEN `docker-compose up` is executed, THEN the Odoo service SHALL successfully connect to the PostgreSQL 15.x database service, create its initial database, and persist all Odoo data across container restarts. | Test: Run `docker-compose up -d`, create a test record in Odoo, then `docker-compose down`, then `docker-compose up -d` again, and verify the test record persists. | Must |
| REQ-052 | Вимога нечітка: 'Конфігурація' не визначає, що саме має бути налаштовано, які параметри чи цілі. Неможливо верифікувати без критеріїв. | WHEN the `machine_bridge` environment is started, THEN the `machine_bridge/docker-compose.yml` file SHALL correctly define and provision all necessary services, networks, and volumes for the `machine_bridge` application to operate. | Inspect the `docker-compose.yml` file for correct syntax and content; Test by executing `docker-compose up` and verifying all services start successfully. | Must |
| REQ-052 | Відсутній метод верифікації та критерії успіху. Як перевірити, що конфігурація 'правильна' чи 'достатня'? | WHEN the `machine_bridge` environment is started using `docker-compose up`, THEN all defined services SHALL start successfully and be accessible, as specified in `machine_bridge/docker-compose.yml`. | Test by executing `docker-compose up` and monitoring logs for successful service startup (exit code 0 or 'healthy' status) for 100% of services. | Must |
| REQ-052 | Відсутня пріоритезація (MoSCoW). | The `machine_bridge/docker-compose.yml` file SHALL be present and correctly define the services, networks, and volumes required for the `machine_bridge` application to function. | Inspect the file's presence and content; Test by deploying and running the application. | Must |
| REQ-053 | Вимога занадто поверхнева: 'Наявність' не визначає вміст, функціонал або цілі Dockerfile. Це не функціональна вимога, а артефакт. | WHEN the `machine_bridge` application image is built, THEN the `machine_bridge/Dockerfile` SHALL correctly define the steps to create a Docker image containing the `machine_bridge` application and all its runtime dependencies, enabling successful execution. | Inspect the `Dockerfile` content for best practices and correct dependencies; Test by building the image and running a container to verify application startup. | Must |
| REQ-053 | Відсутні критерії якості чи функціональності Dockerfile. Як перевірити, що він 'правильний'? | WHEN a Docker image is built from `machine_bridge/Dockerfile`, THEN a container instantiated from this image SHALL successfully start the `machine_bridge` application and respond to its primary health check endpoint with a 200 OK status within 10 seconds of container startup. | Test by building the image, running a container, and executing a health check (e.g., `curl localhost:8080/health`) to confirm application responsiveness and status code. | Must |
| REQ-053 | Відсутня пріоритезація (MoSCoW). | WHEN the `machine_bridge` service is deployed, THEN it SHALL be containerized using a Dockerfile located at `machine_bridge/Dockerfile` in the repository root. | Inspect: Verify the existence and valid syntax of the `machine_bridge/Dockerfile` in the specified location. | Must |
| REQ-055 | Вимога занадто високорівнева та нечітка: 'функціонал обліку' та 'вплив на собівартість' не містять конкретних сценаріїв, правил чи очікуваної поведінки. | WHEN a production order (MO) incurs scrap, THEN the system SHALL allow a user to record the scrap quantity (in units) and select a predefined scrap reason (e.g., 'Material Defect', 'Machine Error') for that MO. | Test: Record 3 units of scrap with 'Material Defect' for an MO. Verify the scrap data is stored and displayed correctly on the MO details screen. | Must |
| REQ-055 | Порушення Singular: вимога об'єднує два значні функціональні блоки ('облік браку' та 'вплив на собівартість'), які слід розділити. | WHEN scrap is recorded for a production order (MO), THEN the system SHALL automatically recalculate the total cost of that MO, incorporating the cost of the scrapped items with an accuracy of at least 2 decimal places. | Test: Record 5 units of scrap for an MO with a known unit cost. Verify that the MO's total cost is updated correctly according to the scrap cost calculation rules. | Must |
| REQ-055 | Відсутній метод верифікації та критерії приймання. Неможливо перевірити, чи 'реалізовано функціонал' без визначених тестів чи результатів. | WHEN a production operation is completed, THEN the system SHALL allow a user with 'Production Operator' role to record up to 5 distinct scrap events, each including quantity (integer units) and a predefined reason, for that operation. | Test: As a 'Production Operator', complete an operation and record 4 distinct scrap events. Verify all 4 events are stored and retrievable via the operation's details. | Must |
| REQ-056 | Відсутнє визначення 'норми' та 'часу виконання', що робить вимогу неповною та нечіткою. | IF the actual execution time for any production operation exceeds 120% of its predefined standard time, THEN the system SHALL send an email notification to the assigned Production Manager within 5 minutes of the operation's completion. | Test: Configure an operation with a standard time of 60 minutes. Simulate an actual execution time of 75 minutes. Verify the Production Manager receives the email notification within 5 minutes. | Should |
| REQ-056 | Не вказано тип, зміст та канал сповіщення для менеджера. | WHEN a task's actual execution time exceeds 120% of its normative time, THEN the system SHALL send an email to the assigned manager. The email SHALL contain the task ID, name, actual time, normative time, and overrun percentage. | Test: Simulate a task (ID: T123, Norm: 100min, Actual: 121min). Verify an email is sent to the manager (e.g., manager@example.com) within 5 minutes, containing 'T123', 'Task Name', '121min', '100min', '21%'. | Must |
| REQ-056 | Відсутній явний метод верифікації для системи сповіщень. | WHEN a task's actual execution time exceeds 120% of its normative time, THEN the system SHALL send an email to the assigned manager. The email SHALL contain the task ID, name, actual time, normative time, and overrun percentage. | Test: Simulate a task (ID: T123, Norm: 100min, Actual: 121min). Verify an email is sent to the manager (e.g., manager@example.com) within 5 minutes, containing 'T123', 'Task Name', '121min', '100min', '21%'. | Must |
| REQ-057 | Відсутнє визначення 'кількості браку' та 'встановленого ліміту', що робить вимогу неповною та нечіткою. | WHEN the number of defective units reported for a production batch exceeds a configurable limit (default: 5 units or 2% of batch size), THEN the system SHALL trigger a notification. | Test: Configure the defect limit to 5 units. Simulate a production batch (ID: B456) with 6 defective units. Verify that a notification is triggered (e.g., an entry in the system's notification log). | Must |
| REQ-057 | Не вказано тип, зміст та канал сповіщення для менеджера. | WHEN the number of defective units reported for a production batch exceeds a configurable limit (default: 5 units or 2% of batch size), THEN the system SHALL send an email to the production manager. The email SHALL contain the batch ID, product name, total units, defective units, and configured limit. | Test: Configure the defect limit to 5 units. Simulate a batch (ID: B456, Product: Widget, Total: 100, Defective: 6). Verify an email is sent to the production manager (e.g., prod_manager@example.com) within 2 minutes, containing 'B456', 'Widget', '100', '6', '5'. | Must |
| REQ-057 | Відсутній явний метод верифікації для системи сповіщень. | WHEN the number of defective units reported for a production batch exceeds a configurable limit (default: 5 units or 2% of batch size), THEN the system SHALL send an email to the production manager. The email SHALL contain the batch ID, product name, total units, defective units, and configured limit. | Test: Configure the defect limit to 5 units. Simulate a batch (ID: B456, Product: Widget, Total: 100, Defective: 6). Verify an email is sent to the production manager (e.g., prod_manager@example.com) within 2 minutes, containing 'B456', 'Widget', '100', '6', '5'. | Must |
| REQ-058 | Нечітке визначення 'автоматичного визначення': незрозуміло, як саме воно працює, джерела даних, критерії успіху. | WHEN a new Modbus device is detected, THEN the system SHALL automatically attempt to identify its Modbus registers by scanning common register ranges and cross-referencing with an internal database of known Modbus profiles. | Inspect (review system logs for the identification process and database lookups for 5 different machine types). | Must |
| REQ-058 | Відсутні критерії верифікації та метод перевірки успішності визначення регістрів. | The automatic Modbus register identification process SHALL correctly map at least 95% of critical operational registers for at least 10 pre-defined machine models within 30 seconds of device connection. | Test (connect each of the 10 pre-defined machine models and verify the accuracy and completeness of identified registers against their specifications). | Must |
| REQ-058 | Не описано поведінку системи для нових або невідомих машин. | IF the automatic Modbus register identification fails to achieve 90% accuracy or identifies the machine as unknown, THEN the system SHALL log the event, notify an administrator via email within 5 minutes, and allow manual configuration of registers. | Test (simulate connection of an unknown Modbus device and a known device with corrupted profile; verify log, email, and manual configuration option). | Should |
| REQ-059 | Нечітке визначення 'інтеграції': не вказано, які саме дані чи функції будуть інтегровані, мета інтеграції. | WHEN a new Heidelberg machine with Prinect system is connected via OPC-UA, THEN the system SHALL establish an OPC-UA client connection to retrieve real-time production data including job ID, current print count, machine status (running/idle/error), and error codes. | Inspect (review system data points for presence and correct values of specified production data from a connected machine). | Must |
| REQ-059 | Відсутні критерії верифікації та метод перевірки успішності інтеграції. | The OPC-UA integration for Heidelberg machines with Prinect SHALL successfully retrieve and update all specified real-time production data points with a latency of no more than 5 seconds, maintaining data integrity for at least 99% of data points over a 24-hour period. | Analyze (monitor data flow and integrity for a connected machine over 24 hours), Test (simulate data changes on the OPC-UA server and verify updates in the system within 5 seconds). | Must |
| REQ-059 | Не описано обробку помилок або крайні випадки під час взаємодії з OPC-UA. | WHEN the system interacts with an OPC-UA server for Heidelberg machines, IF a connection error, data validation error, or timeout (exceeding 5 seconds) occurs, THEN the system SHALL log the error with severity and details, and attempt to re-establish the connection up to 3 times before alerting an administrator. | Test (simulate connection loss, invalid data, timeout) / Inspect (logs for error entries and retry attempts). | Must |
| REQ-060 | Нечітке визначення 'автоматичного розгортання': не вказано цільові середовища, тригери, кроки розгортання. | WHEN a successful merge to the 'main' branch occurs, THEN the CI/CD pipeline SHALL automatically build, test, and deploy the latest application version to the 'Staging' environment within 5 minutes. | Demo (trigger a merge to 'main', observe pipeline execution and deployment to 'Staging') / Inspect (pipeline logs for completion time). | Must |
| REQ-060 | Відсутні критерії верифікації та метод перевірки успішності розгортання (наприклад, час, відсутність простоїв). | WHILE deploying to the 'Production' environment, the CI/CD pipeline SHALL ensure zero downtime for end-users, and the entire deployment process SHALL complete within 10 minutes. | Test (monitor application availability during deployment via automated health checks) / Analyze (deployment logs for total duration). | Must |
| REQ-060 | Не описано стратегію розгортання (наприклад, blue/green, canary) та обробку відкатів. | IF a deployment to 'Production' fails (e.g., health check fails for > 30 seconds after deployment), THEN the CI/CD pipeline SHALL automatically initiate a rollback to the previous stable version using a blue/green strategy within 2 minutes. | Test (simulate a deployment failure in 'Production', observe automatic rollback to the previous version) / Inspect (pipeline configuration for rollback strategy). | Must |
| REQ-061 | Вимога є надто високорівневою фічею, а не атомарною вимогою. Незрозуміло, що саме має бути реалізовано. | WHEN a user selects 'Управління складом паперу', THEN the system SHALL display a user interface allowing the user to define, view, edit, and delete storage zones and racks, each identified by a unique ID. | Demo (user interaction with the UI to perform CRUD operations on zones and racks) / Test (backend API for zone/rack management). | Should |
| REQ-061 | Відсутні критерії верифікації та вимірні показники успіху. Неможливо перевірити виконання. | WHEN a user assigns paper stock to a storage location, THEN the system SHALL record the specific storage zone and rack for that paper stock item. The system SHALL display the assigned location within 2 seconds of assignment. | Test: Assign 5 different paper stock items to various zones/racks. Verify that the system accurately displays the assigned location for each item within the specified time. | Must |
| REQ-061 | Не є сингулярною, об'єднує кілька нечітко визначених функцій/механізмів (управління, зони, стелажі, QR/штрихкод). | WHILE paper stock is stored in the warehouse, THEN each individual paper stock item SHALL be uniquely identifiable by a system-generated QR code or barcode. The system SHALL ensure uniqueness across all active stock items. | Test: Generate and print QR/barcodes for 10 different paper stock items. Verify that each code is unique and scannable by the system. | Must |
| REQ-062 | Відсутні критерії верифікації та вимірні показники успіху. Неможливо перевірити виконання. | WHEN a Sale Order (SO) transitions to "Confirmed" status AND contains line items requiring production, THEN the system SHALL automatically create a Manufacturing Order (MO) for each such line item. This process SHALL complete within 5 seconds for SOs with up to 10 production line items. | Test: Create 3 Sale Orders (1, 5, 10 production line items), confirm them, and verify that corresponding MOs are created with correct data within the specified time. | Must |
| REQ-062 | Вимога неповна: не визначено тригери, правила створення MO, обробка помилок або невалідних Sale Order. | WHEN a Sale Order (SO) transitions to "Confirmed" status AND contains line items requiring production, THEN the system SHALL automatically create a Manufacturing Order (MO) for each such line item. IF the automatic MO creation fails (e.g., due to invalid product data), THEN the system SHALL log the error with a unique ID and notify the "Production Manager" role via email within 1 minute. | Test: Create an SO with a valid production item, confirm, verify MO. Create an SO with an invalid production item, confirm, verify error log and email notification to Production Manager. | Must |
| REQ-062 | Неоднозначність у визначенні "автоматичного створення" та його бізнес-логіки. | WHEN a Sale Order (SO) is marked as "Confirmed", THEN the system SHALL automatically initiate the creation of a Manufacturing Order (MO) for each line item that has its "Requires Production" flag set to TRUE. The MO SHALL inherit the product ID, quantity, and requested completion date from the corresponding SO line item. | Test: Create an SO with multiple line items (some requiring production, some not). Confirm the SO. Inspect the created MOs to ensure only production items triggered MOs and that inherited data is correct. | Must |
| gaps | {"id": "G001", "description": "Недостатньо деталей для 'Старт роботи над WO': відсутні критерії успішного старту, фіксовані дані, попередні умови."} | WHEN a user initiates 'Start Work' on a Work Order (WO), THEN the system SHALL record the start timestamp, the user ID, and change the WO status to 'In Progress'. IF the WO is not in 'Ready' or 'Paused' status, THEN the system SHALL prevent the start operation and display an error message. | Test (verify status change, timestamp, user ID, error handling for invalid WO status). | Must |
| gaps | {"id": "G002", "description": "Недостатньо деталей для 'Пауза', 'Резюм після паузи', 'Стоп': відсутній опис поведінки, станів, даних, що фіксуються."} | WHEN a user initiates 'Pause Work' on an 'In Progress' WO, THEN the system SHALL record the pause timestamp, the user ID, change the WO status to 'Paused', and stop time tracking. WHEN a user initiates 'Resume Work' on a 'Paused' WO, THEN the system SHALL record the resume timestamp, the user ID, change the WO status to 'In Progress', and restart time tracking. WHEN a user initiates 'Stop Work' on an 'In Progress' or 'Paused' WO, THEN the system SHALL record the stop timestamp, the user ID, change the WO status to 'Completed', stop time tracking, and calculate the total work duration. | Test (verify status changes, timestamps, user IDs, and time tracking logic for pause, resume, and stop actions). | Must |
| gaps | {"id": "G003", "description": "Недостатньо деталей для 'Time-based progress bar': відсутній опис логіки розрахунку прогресу."} | WHILE a Work Order is 'In Progress', the system SHALL display a time-based progress bar. The progress bar SHALL be calculated as (actual elapsed time / estimated total time) * 100%. IF the actual elapsed time exceeds the estimated total time, THEN the progress bar SHALL show 100% and visually indicate an overrun. | Test (verify progress bar calculation with various time inputs and visual representation for normal and overrun scenarios). | Should |
| gaps | {"id": "G004", "description": "Недостатньо деталей для 'Push результатів в Odoo через REST': відсутній опис формату даних та цільових ендпоінтів."} | WHEN a Work Order is completed, THEN the system SHALL push the WO completion results (including WO ID, actual start/stop timestamps, total duration, user ID) to Odoo via a REST API. The data SHALL be formatted as JSON, and the target endpoint SHALL be `/api/v1/work_orders/completion`. The system SHALL retry the push up to 3 times with a 5-second delay between retries IF the initial push fails. | Test (verify successful data push to Odoo, data format, target endpoint, and retry mechanism for failed pushes). | Must |
| gaps | {"id": "G005", "description": "Недостатньо деталей для 'Алерти менеджеру (час > 120% норми)': відсутнє визначення 'норми' та способу доставки алертів."} | WHILE a Work Order is 'In Progress', IF the actual elapsed time exceeds 120% of the estimated total time, THEN the system SHALL send an alert to the assigned manager. The alert SHALL include the WO ID, current elapsed time, estimated total time, and the percentage overrun. The alert SHALL be delivered via email to the manager's registered email address. | Test (verify alert trigger condition at 120% threshold, content accuracy, and successful email delivery to the manager). | Should |
| gaps | {"id": "G006", "description": "Недостатньо деталей для 'Алерти менеджеру (брак > ліміту)': відсутнє визначення 'браку', 'ліміту' та способу доставки алертів."} | WHEN the quantity on hand for any inventory item falls below its configured minimum stock level, THEN the system SHALL send an email alert to the designated inventory manager. The alert email SHALL include the item's name, current quantity, and the configured minimum stock level. | Test: Reduce an item's quantity below its configured minimum stock level and verify that an email alert is sent to the designated manager with correct item details (item name, current quantity, minimum stock level). | Must |

## Джерела (цитувати у звіті)
- ISO/IEC/IEEE 29148:2018 — Systems and software engineering — Requirements engineering. Шаблон/огляд: https://www.well-architected-guide.com/documents/iso-iec-ieee-29148-template/ ; https://www.cwnp.com/req-eng/
- EARS (Easy Approach to Requirements Syntax), Mavin et al. — шаблони WHEN/WHILE/IF-THEN/WHERE.
- Spec-Driven Development: From Code to Contract in the Age of AI Coding Assistants — https://arxiv.org/html/2602.00180v1
- Spec-Driven Development: The Definitive 2026 Guide — https://thebcms.com/blog/spec-driven-development
- How To Write Clear, Measurable Software Requirements in 2026 — https://www.designrush.com/agency/software-development/trends/software-requirements-specification
- Requirements Smells (Femmer et al.) — https://arxiv.org/pdf/1611.08847
- Well-Formed Quality of System Requirements for ISO 29148-2018 (NLP metric) — https://www.researchgate.net/publication/385802396
- QUARE: Quality-Aware Requirements Analysis through Multi-Agent Dialectical Negotiation — https://arxiv.org/pdf/2603.11890
